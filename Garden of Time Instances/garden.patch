diff --git a/src/atelier/script_constants.hpp b/src/atelier/script_constants.hpp
index b14c6e26d..68d8e5e79 100644
--- a/src/map/script_constants.hpp
+++ b/src/map/script_constants.hpp
@@ -5,10 +5,21 @@
 	/* Mob skills */
+	export_constant(UNT_SEED_TRAP);

+	export_constant(SC_BLOCK_SEAL);
+	export_constant(SC_TARGET_MARKER);
+	export_constant(SC_FROST_STORM);
+	export_constant(SC_GROGGY);
+	export_constant(SC_WARM_SHIELD);

+	/* Mapflags */
+	export_constant(MF_MD_SELFDESTRUCTION);
+
 
   #undef export_constant
 #endif
diff --git a/src/map/battle.cpp b/src/map/battle.cpp
index 450b0610d..9b0be15de 100644
--- a/src/map/battle.cpp
+++ b/src/map/battle.cpp
@@ -2040,8 +2040,8 @@ int64 battle_calc_damage(struct block_list *src,struct block_list *bl,struct Dam
 	if (bl->type == BL_MOB) { // Reduces damage received for Green Aura MVP
 		mob_data *md = BL_CAST(BL_MOB, bl);
 
-		if (md && md->damagetaken != 100)
-			damage = i64max(damage * md->damagetaken / 100, 1);
+		if (md && md->damagetaken != 10000)
+			damage = i64max(damage * md->damagetaken / 10000, 1);
 	}
 	
 	if (tsc != nullptr && !tsc->empty()) {
@@ -6663,6 +6663,10 @@ static int32 battle_calc_attack_skill_ratio(struct Damage* wd, struct block_list
 			if (wd->miscflag & SKILL_ALTDMG_FLAG)
 				skillratio = skillratio * 3 / 10;
 			break;
+		case NPC_AIMED_SHOWER:
+		case NPC_BLAZING_ERUPTION:
+			//Balance me!
+			skillratio += -100 + 4000 + 200 * skill_lv;
+			break;
 	}
 	return skillratio;
 }
@@ -9331,6 +9335,15 @@ struct Damage battle_calc_magic_attack(struct block_list *src,struct block_list
 						if (mflag & SKILL_ALTDMG_FLAG)
 							skillratio = skillratio * 3 / 10;
 						break;
+                   //Balance me!
+					case NPC_BLOCK_EXPLOSION:
+						skillratio += -100 + 3000 + 200 * skill_lv;
+						break;
+					case NPC_LIGHTNING_JUDGEMENT:
+						skillratio += -100 + 2000 + 200 * skill_lv;
+						if ( tsc && tsc->getSCE(SC_FROST_STORM) ) {
+							skillratio += 3000;
+						}
+						break;
 				}
 
 				if (sc) {// Insignia's increases the damage of offensive magic by a fixed percentage depending on the element.
@@ -9935,6 +9948,9 @@ struct Damage battle_calc_misc_attack(struct block_list *src,struct block_list *
 		case NPC_LOCKON_LASER_ATK:
 			md.damage = (10000 + (20000 * skill_lv));
 			break;
+		case NPC_SEEDTRAP:
+			md.damage = 10000 + (10000 * skill_lv) - (5000 * (rand() % (skill_lv + 2)));
+			break;
 	}
 
 	if (nk[NK_SPLASHSPLIT]) { // Divide ATK among targets
diff --git a/src/map/map.hpp b/src/map/map.hpp
index 7bc49447a..5924b30e5 100644
--- a/src/map/map.hpp
+++ b/src/map/map.hpp
@@ -687,6 +687,10 @@ enum e_mapflag : int16 {
 	MF_NOBANK,
 	MF_SPECIALPOPUP,
 	MF_NOMACROCHECKER,
+	
+	MF_MD_SELFDESTRUCTION,
 	MF_MAX
 };
 
diff --git a/src/map/mob.cpp b/src/map/mob.cpp
index 935397fd0..56acb88e7 100644
--- a/src/map/mob.cpp
+++ b/src/map/mob.cpp
@@ -4951,7 +4951,7 @@ s_mob_db::s_mob_db()
 	this->vd = {};
 	this->option = {};
 	this->skill = {};
-	this->damagetaken = 100;
+	this->damagetaken = 10000;
 	this->group_id = {};
 	this->title = {};
 }
@@ -5402,7 +5402,7 @@ uint64 MobDatabase::parseBodyNode(const ryml::NodeRef& node) {
 	if (this->nodeExists(node, "DamageTaken")) {
 		uint16 damage;
 
-		if (!this->asUInt16Rate(node, "DamageTaken", damage, 100))
+		if (!this->asUInt16Rate(node, "DamageTaken", damage, 10000))
 			return 0;
 
 		mob->damagetaken = damage;
diff --git a/src/map/npc.cpp b/src/map/npc.cpp
index b3576eeb5..3026e3d9d 100644
--- a/src/map/npc.cpp
+++ b/src/map/npc.cpp
@@ -1084,6 +1084,8 @@ bool npc_enable_target(npc_data& nd, uint32 char_id, e_npcv_status flag)
 			nd.sc.option |= OPTION_HIDE;
 			nd.is_invisible = true;
 		}
+		if (nd.ud.state.force_walk)
+			nd.ud.state.force_walk = false;
 		if (nd.class_ != JT_WARPNPC && nd.class_ != JT_GUILD_FLAG) {	//Client won't display option changes for these classes [Toms]
 			clif_changeoption(&nd);
 			if (nd.is_invisible)
diff --git a/src/map/skill.cpp b/src/map/skill.cpp
index 42301e154..b7e238523 100755
--- a/src/map/skill.cpp
+++ b/src/map/skill.cpp
@@ -7509,6 +7509,26 @@ int32 skill_castend_damage_id (struct block_list* src, struct block_list *bl, ui
 		clif_skill_nodamage(src, *bl, skill_id, skill_lv);
 		skill_attack(skill_get_type(skill_id), src, src, bl, skill_id, skill_lv, tick, flag);
 		break;
+		
+	case NPC_AIMED_SHOWER:
+	case NPC_BLAZING_ERUPTION:
+		if ( !(tsc && (tsc->getSCE(SC_TARGET_MARKER)) ) ) {
+			break;
+		}
+		status_change_end(bl, SC_TARGET_MARKER);
+		skill_attack(skill_get_type(skill_id), src, src, bl, skill_id, skill_lv, tick, flag);
+		break;
+	case NPC_LIGHTNING_JUDGEMENT:
+		skill_attack(skill_get_type(skill_id), src, src, bl, skill_id, skill_lv, tick, flag);
+		break;	
+	case NPC_BLOCK_EXPLOSION:
+		if ( !(tsc && (tsc->getSCE(SC_BLOCK_SEAL)) ) ) {
+			break;
+		}
+		status_change_end(bl, SC_BLOCK_SEAL);
+		skill_attack(skill_get_type(skill_id), src, src, bl, skill_id, skill_lv, tick, flag);
+		break;
 
 	default:
 		ShowWarning("skill_castend_damage_id: Unknown skill used:%d\n",skill_id);
@@ -8395,6 +8415,7 @@ int32 skill_castend_nodamage_id (struct block_list *src, struct block_list *bl,
 
 	case NPC_GRADUAL_GRAVITY:
 	case NPC_DEADLYCURSE:
+	case NPC_GROGGY_ON:
 		status_change_start(src, bl, type, 10000, skill_lv, 0, 0, 0, skill_get_time(skill_id, skill_lv), SCSTART_NOAVOID|SCSTART_NOTICKDEF|SCSTART_NORATEDEF);
 		clif_skill_nodamage(src, *bl, skill_id, skill_lv);
 		break;
@@ -9222,7 +9243,7 @@ int32 skill_castend_nodamage_id (struct block_list *src, struct block_list *bl,
 	case NPC_SELFDESTRUCTION:
 		//Self Destruction hits everyone in range (allies+enemies)
 		//Except for Summoned Marine spheres on non-versus maps, where it's just enemies and your own slaves.
-		if ((md == nullptr || md->special_state.ai == AI_SPHERE) && !map_flag_vs(src->m)) {
+		if ((md == nullptr || md->special_state.ai == AI_SPHERE) && !map_flag_vs(src->m) || (md && map_getmapflag(src->m, MF_MD_SELFDESTRUCTION))) {
 			// Enable Marine Spheres to damage own Homunculus and summons outside PVP
 			if (battle_config.alchemist_summon_setting&8)
 				i = BCT_ENEMY|BCT_SLAVE;
@@ -11625,6 +11646,33 @@ int32 skill_castend_nodamage_id (struct block_list *src, struct block_list *bl,
 				src, skill_id, skill_lv, tick, flag|BCT_PARTY|1, skill_castend_nodamage_id);
 		break;
 
+	case NPC_RESET_EFST:
+		clif_skill_nodamage(src,*bl,skill_id,skill_lv);
+		
+		if(tsc == nullptr || tsc->empty())
+			break;
+
+		for (const auto &it : status_db) {
+			sc_type status = static_cast<sc_type>(it.first);
+
+			if (!tsc->getSCE(status))
+				continue;
+
+			if (it.second->flag[SCF_NOCLEARANCE])
+				continue;
+
+			status_change_end(bl,status);
+		}
+		break;
+		
 	case AB_CLEARANCE:
 		if( flag&1 || (i = skill_get_splash(skill_id, skill_lv)) < 1 ) { // As of the behavior in official server Clearance is just a super version of Dispell skill. [Jobbie]
@@ -13682,6 +13734,51 @@ int32 skill_castend_nodamage_id (struct block_list *src, struct block_list *bl,
 			map_foreachinrange( skill_area_sub, bl, range, BL_CHAR, src, skill_id, skill_lv, tick, flag | BCT_ENEMY | SD_SPLASH | 1, skill_castend_nodamage_id );
 		}
 		break;
+		
+	case NPC_TARGET_MARKER:
+		if( flag&1 )
+			status_change_start(src, bl, skill_get_sc(skill_id), 10000, skill_lv, 0, 0, 0, skill_get_time(skill_id, skill_lv), SCSTART_NOAVOID|SCSTART_NOTICKDEF|SCSTART_NORATEDEF);
+		else {
+			map_foreachinallrange(skill_area_sub, src, skill_get_splash(skill_id, skill_lv), BL_CHAR,
+				src, skill_id, skill_lv, tick, flag|BCT_ENEMY|1, skill_castend_nodamage_id);
+			clif_skill_nodamage(src, *bl, skill_id, skill_lv);
+		}
+		break;
+		
+	case NPC_BLOCK_SEAL:
+		if( flag&1 ) {
+			if ( !(tsc && (tsc->getSCE(SC_TARGET_MARKER)) ) ) {
+				break;
+			}
+			status_change_end(bl, SC_TARGET_MARKER);
+			status_change_start(src, bl, skill_get_sc(skill_id), 10000, skill_lv, 0, 0, 0, skill_get_time(skill_id, skill_lv), SCSTART_NOAVOID|SCSTART_NOTICKDEF|SCSTART_NORATEDEF);
+		}
+		else {
+			map_foreachinallrange(skill_area_sub, src, skill_get_splash(skill_id, skill_lv), BL_CHAR,
+				src, skill_id, skill_lv, tick, flag|BCT_ENEMY|1, skill_castend_nodamage_id);
+			clif_skill_nodamage(src, *bl, skill_id, skill_lv);
+		}
+		break;
+		
+	case NPC_FROST_FIELD:
+		if( flag&1 ) {
+			if ( tsc && !tsc->getSCE(SC_WARM_SHIELD) )
+				status_change_start(src, bl, skill_get_sc(skill_id), 10000, skill_lv, 0, 0, 0, skill_get_time(skill_id, skill_lv), SCSTART_NOAVOID|SCSTART_NOTICKDEF|SCSTART_NORATEDEF);
+		}
+		else {
+			map_foreachinallrange(skill_area_sub, src, skill_get_splash(skill_id, skill_lv), BL_CHAR,
+				src, skill_id, skill_lv, tick, flag|BCT_ENEMY|1, skill_castend_nodamage_id);
+			clif_skill_nodamage(src, *bl, skill_id, skill_lv);
+		}
+		break;
+		
+	case NPC_AIMED_SHOWER:
+	case NPC_BLAZING_ERUPTION:
+	case NPC_LIGHTNING_JUDGEMENT:
+	case NPC_BLOCK_EXPLOSION:
+		map_foreachinallrange(skill_area_sub, src, skill_get_splash(skill_id, skill_lv), BL_CHAR, src, skill_id, skill_lv, tick, flag|BCT_ENEMY|1, skill_castend_damage_id);
+		clif_skill_nodamage(src, *src, skill_id, skill_lv);
+		break;
 
 	default: {
 		std::shared_ptr<s_skill_db> skill = skill_db.find(skill_id);
@@ -15677,6 +15774,7 @@ int32 skill_castend_pos2(struct block_list* src, int32 x, int32 y, uint16 skill_
 		clif_blown(src);
 		break;
 	
+	case NPC_SEEDTRAP:
 	case NPC_LOCKON_LASER_ATK:
 		skill_unitsetting(src, skill_id, skill_lv, x, y, flag);
 		break;
@@ -21649,6 +21747,13 @@ static int32 skill_trap_splash(struct block_list *bl, va_list ap)
 		case UNT_REVERBERATION: // For proper skill delay animation when used with Dominion Impulse
 			skill_addtimerskill(ss, tick + 50, bl->id, 0, 0, NPC_REVERBERATION_ATK, sg->skill_lv, BF_WEAPON, 0);
 			break;
+		case UNT_SEED_TRAP:
+		
+			if( skill_attack(skill_get_type(sg->skill_id),ss,src,bl,sg->skill_id,sg->skill_lv,tick,sg->val1) ) {
+				int32 dval = distance_bl(src, bl);
+				skill_blown(src, bl, max(0, (7 - dval)), -1, BLOWN_NONE);
+			}
+			break;
 		case UNT_FIRINGTRAP:
 		case UNT_ICEBOUNDTRAP:
 			if( src->id == bl->id ) break;
@@ -22592,6 +22697,15 @@ static int32 skill_unit_timer_sub(DBKey key, DBData *data, va_list ap)
 					skill_delunit(unit);
 				}
 				break;
+			
+			case UNT_SEED_TRAP: {
+				clif_changetraplook(bl,UNT_USED_TRAPS);
+				map_foreachinrange(skill_trap_splash, bl, skill_get_splash(group->skill_id, group->skill_lv), group->bl_flag, bl, tick);
+				group->limit = DIFF_TICK(tick,group->tick) + 1000;
+				unit->limit = DIFF_TICK(tick,group->tick) + 1000;
+				group->unit_id = UNT_USED_TRAPS;
+				skill_delunit(unit);
+			} break;
 
 			default:
 				if (group->val2 == 1 && (group->skill_id == WZ_METEOR || group->skill_id == SU_CN_METEOR || group->skill_id == SU_CN_METEOR2 || 
diff --git a/src/map/skill.hpp b/src/map/skill.hpp
index 17f0f79f1..3c8f29104 100644
--- a/src/map/skill.hpp
+++ b/src/map/skill.hpp
@@ -2731,8 +2731,9 @@ enum e_skill_unit_id : uint16 {
 	UNT_TWINKLING_GALAXY,
 	UNT_STAR_CANNON,
 	UNT_GRENADES_DROPPING,
+	
+	UNT_SEED_TRAP = 268,
 
 	UNT_FUUMASHOUAKU = 290, // Huuma Shuriken - Grasp
 	UNT_MISSION_BOMBARD,
diff --git a/src/map/status.cpp b/src/map/status.cpp
index 7af0da850..05831381a 100644
--- a/src/map/status.cpp
+++ b/src/map/status.cpp	
@@ -9562,6 +9565,7 @@ static int32 status_get_sc_interval(enum sc_type type)
 		case SC_KILLING_AURA:
 		case SC_BOSSMAPINFO:
 		case SC_LOCKON_LASER:
+		case SC_GROGGY:
 			return 1000;
 		case SC_WINKCHARM:
 		case SC_VOICEOFSIREN:
@@ -11146,6 +11150,7 @@ static bool status_change_start_post_delay(block_list* src, block_list* bl, sc_t
 		case SC_WINKCHARM:
 		case SC_VOICEOFSIREN:
 		case SC_LOCKON_LASER:
+		case SC_GROGGY:
 			tick_time = status_get_sc_interval(type);
 			val4 = tick - tick_time; // Remaining time
 			break;
diff --git a/src/map/status.hpp b/src/map/status.hpp
index b467eea6a..c518fa46f 100644
--- a/src/map/status.hpp
+++ b/src/map/status.hpp
@@ -1442,6 +1442,11 @@ enum sc_type : int16 {
 #ifdef ATELIER
 	SC_SERVER_BLESSING,
 	SC_LOCKON_LASER,
+	SC_TARGET_MARKER,
+	SC_BLOCK_SEAL,
+	SC_FROST_STORM,
+	SC_GROGGY,
+	SC_WARM_SHIELD,
 #endif
 
 	SC_MAX, //Automatically updated max, used in for's to check we are within bounds.
